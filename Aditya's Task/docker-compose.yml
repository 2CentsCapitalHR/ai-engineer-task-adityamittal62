version: "3.9"
services:
  adgm-corporate-agent:
    build: .
    ports:
      - "7860:7860"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - USE_OPENAI_EMBEDDINGS=1
    volumes:
      - .:/app
